
<% other.each do |post| %>
  <div class="col-xs-3 hovereffect">
    <%= link_to post_path(id: post.id), class: 'info' do %>
      <%= image_tag(post.image, class: 'img-responsive') %>
      <div class="overlay">
          <h2><%= post.title %></h2>
      </div>
    <% end %>
  </div>
<% end %>
